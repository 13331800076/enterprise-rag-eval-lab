version: "3.8"

services:
  rag-lab:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./examples:/app/examples:ro
      - ./tests/fixtures:/app/tests/fixtures:ro
    command: uvicorn rag_lab.api.app:app --host 0.0.0.0 --port 8000 --reload
